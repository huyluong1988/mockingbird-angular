import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'mocking-bird-tasks-app-mobile',
  webDir: 'dist/mocking-bird-tasks-app-mobile'
};

export default config;
